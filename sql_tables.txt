CREATE TABLE monitoring (
	id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
	time TEXT(40) NOT NULL,
	sensor TEXT(40) NOT NULL,
	count INTEGER NOT NULL
);

CREATE TABLE errors (
	id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
	time TEXT(40) NOT NULL,
	sensor TEXT(40) NOT NULL,
	error INTEGER NOT NULL
);
